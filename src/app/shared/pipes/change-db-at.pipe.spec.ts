import { ChangeDbAtPipe } from './change-db-at.pipe';

describe('ChangeDbAtPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDbAtPipe();
    expect(pipe).toBeTruthy();
  });
});
